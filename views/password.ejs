<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>Document</title>
	<style >
		*{	
			margin: 0;
			padding: 0;
			box-sizing: border-box;
		}
		body{
			width: 100%;
			height: 100%;
			background:#ffe6e6;
		}

		.all{
			position: absolute;
			border: 1px solid #333;
			border-radius: 10px;
			width: 30%;
			height: 30%;
			margin:20% 35%;
			display: flex;
			flex-direction: column;
			justify-content: space-between;
			padding: 10px;
			background:#ffcccc;
		}
		label{
			margin-top:10px ;
			text-align: center;
			text-transform: uppercase;
			font-size: 15px;
			font-weight: bold;
		}
		button{
			width: 100%;
  			background-color: #4CAF50;
  			color: white;
  			padding: 14px 20px;
  			margin: 8px 0;
  			border: none;
  			border-radius: 4px;
  			cursor: pointer;
		}
		input{
			width: 100%;
			padding: 12px 20px;
  			margin: 8px 0;
  			display: inline-block;
  			border: 1px solid #ccc;
  			border-radius: 4px;
  			box-sizing: border-box;
		}
		.accept{
			display: flex;
			flex-direction: column;
			margin-bottom:10px ;
		}
		p{
			margin:5px 0;
			text-align: center;
			text-transform: uppercase;
			font-size: 15px;
			font-weight: bold;
			color: red;
		}
	</style>
</head>
<body>
	<!-- if there is no action send it to the exact page -->
	<div class="all">
		<% if(locals.error != null){%>
			<div class="accept">
				<p>Incorrect Password</p>
			</div>
		<% } %>
		<form method="POST">
			<label for="password">Password:</label>
			<input type="password" id="password" name="password" required>
			<button type="submit">Download</button>
		</form>
	</div>
</body>
</html>